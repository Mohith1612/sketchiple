# WebSocket server base URL (without trailing slash, without /ws path)
# Leave unset in development — Vite proxy handles /ws → localhost:3001
# VITE_WS_URL=wss://your-server.example.com

# Maximum reconnect attempts before marking connection as exhausted in UI/debug status
# VITE_WS_MAX_RETRIES=20
